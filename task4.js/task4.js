const calculateRectangleProperties = (width, height) => { const areaRectangle = width * height; 
const perimeterRectangle = 2 * (width + height);
return console.log(`Area of a rectangle = ${areaRectangle}/nPerimeter of a rectangle = ${perimeterRectangle}`);}
calculateRectangleProperties(5, 13);